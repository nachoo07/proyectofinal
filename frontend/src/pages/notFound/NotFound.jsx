import { Typography } from "@mui/material";

const NotFound = () => {
    return (
        <Typography variant="h3">
            Bienvenidos al himalaya, aqui no hay nada (pagina no encontrada)
        </Typography>
    )
}

export default NotFound;